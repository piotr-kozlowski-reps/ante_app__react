import React, { Fragment } from "react";

const Login = () => {
  return (
    <Fragment>
      <div data-testid="login-page"></div>
      <div>{`login`}</div>
    </Fragment>
  );
};

export default Login;
